defmodule ConfigExample do
  @moduledoc """
  Global configuration files for the firmware.

  Note:
    - rename the file into config.ex
    - rename the module name into MMConfig
  """

  def ssid_name(), do: "" # your ssid name
  def ssid_password(), do: "" # your ssid password
end
